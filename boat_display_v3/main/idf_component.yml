dependencies:
  idf: ">=5.1.0"
  
  # The BSP in /components handles LVGL, LCD, and Touch.
  # These are your application-specific needs:
  espressif/esp_websocket_client: "^1.2.0"  # legacy - kept for compatibility
  ### espressif/esp-mqtt: "^2.0.0"
  
  # Explicitly allow the beta version of the C++ components
  espressif/esp-idf-cxx: 
    version: "^1.0.0-beta"
    pre_release: true